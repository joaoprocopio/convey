version: "2"

sql:
    - engine: "postgresql"
      schema: "./internal/db/migrations"
      queries: "./internal/propostas/queries.sql"
      gen:
          go:
              out: "./internal/propostas/queries"
              emit_json_tags: true
              sql_package: "pgx/v5"
