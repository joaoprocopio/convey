version: "2"

sql:
    - engine: "sqlite"
      database:
          uri: "./convey.sqlite3"
      schema: "./internal/db/migrations"
      queries: "./internal/propostas/services/services.sql"
      gen:
          go:
              out: "./internal/propostas/services"
              emit_json_tags: true
